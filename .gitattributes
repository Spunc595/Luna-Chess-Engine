# Shell scripts are stored and exported with LF: a script with CRLF line endings does not run on Linux
# (scripts/sprt_match.sh arrived on the match server with CRLF and its line continuations broke).
*.sh text eol=lf
